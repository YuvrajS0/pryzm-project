# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# SAM.gov API Configuration (Optional)
# Get your API key from: https://sam.gov/content/home
# Navigate to: Account Details > API Key
# Without this key, SAM.gov opportunities will be skipped
SAM_GOV_API_KEY=your-sam-gov-api-key

# Vercel Cron Secret (secures the /api/sync endpoint)
# Generate a random string, e.g.: openssl rand -hex 32
CRON_SECRET=

# Optional: tweak for local development
# NEXT_PUBLIC_APP_URL=http://localhost:3000
